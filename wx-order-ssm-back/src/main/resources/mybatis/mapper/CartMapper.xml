<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wechat.ordering.mapper.CartMapper">

    <resultMap id="CartResultMap" type="Cart">
        <id column="id" property="id" />
        <result column="user-id" property="userId" />
        <result column="dish-id" property="dishId" />
        <result column="quantity" property="quantity" />
        <result column="total-price" property="totalPrice" />
        <result column="create-time" property="createTime" />
    </resultMap>

    <select id="selectAll" resultMap="CartResultMap">
        SELECT id, `user-id`, `dish-id`, quantity, `total-price`, `create-time`
        FROM t_cart
    </select>

    <select id="selectById" parameterType="long" resultMap="CartResultMap">
        SELECT id, `user-id`, `dish-id`, quantity, `total-price`, `create-time`
        FROM t_cart WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="CartResultMap">
        SELECT id, `user-id`, `dish-id`, quantity, `total-price`, `create-time`
        FROM t_cart WHERE `user-id` = #{userId}
    </select>

    <insert id="insert" parameterType="Cart" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_cart (`user-id`, `dish-id`, quantity, `total-price`, `create-time`)
        VALUES (#{userId}, #{dishId}, #{quantity}, #{totalPrice}, #{createTime})
    </insert>

    <update id="update" parameterType="Cart">
        UPDATE t_cart SET quantity = #{quantity}, `total-price` = #{totalPrice}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM t_cart WHERE id = #{id}
    </delete>

</mapper>
